#ifndef SIMULATOR_TYPES_H
#define SIMULATOR_TYPES_H
/// ringBuffer.h requires this header to exist because that is copied from a simulator implementation.
#include <stdint.h>
#include <stdbool.h>
#endif
